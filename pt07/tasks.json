{
  "$schema": "pt07 task manifest (task book §11)",
  "meta": {
    "name": "pt07-baseline",
    "version": "1.0.0",
    "seed": 20260907,
    "fixture": "pt07/workspace (regenerate with: node pt07/generate.mjs)",
    "groundtruth": "pt07/groundtruth.json (regenerated by generate.mjs, deterministic)",
    "defaultModel": "zai/glm-4.5-air",
    "mode": "baseline-no-tm-tools",
    "expectedStepsBand": "5-20 (task book §11); light QA/locate tasks intentionally land at the low end",
    "promptRules": [
      "prompts contain no double quotes (runner passes them as argv)",
      "answers embed stable markers (PT07_ANSWER_*, PT07_TODO) for machine judging",
      "judges recompute expectations from the fixture itself, not from free-form model claims"
    ]
  },
  "tasks": [
    {
      "id": "t01-read-codeqa",
      "category": "read-dominant",
      "title": "Code Q&A across three source files",
      "prompt": "Read these files in the workspace: src/config.js, src/pricing.js and src/inventory.js. Answer three questions. 1) What is the exact numeric value of the TAX_RATE constant in src/config.js? 2) What is the exact numeric value of the SHIPPING_FLAT_FEE constant in src/config.js? 3) Which file exports the round2 helper used by applyCoupon in src/pricing.js? Reply with exactly three lines and nothing else:\nPT07_ANSWER_TAX: <value>\nPT07_ANSWER_SHIPPING: <value>\nPT07_ANSWER_ROUND2_FILE: <file path>",
      "successCriteria": "Reply contains PT07_ANSWER_TAX: 0.18, PT07_ANSWER_SHIPPING: 4.5 and PT07_ANSWER_ROUND2_FILE pointing at src/pricing.js",
      "judge": { "ref": "judge.mjs", "checkId": "reply-substring" },
      "expectedSteps": [5, 10]
    },
    {
      "id": "t02-grep-locate",
      "category": "grep-dominant",
      "title": "Locate all TODO(pt07) markers with file:line",
      "prompt": "Search the source code under src/ for every TODO(pt07) marker comment. Reply with one line per marker in exactly this format, using the path relative to the workspace root and the correct 1-based line number:\nPT07_TODO <path>:<line>\nList all markers. Nothing else in the reply.",
      "successCriteria": "Set of <path>:<line> pairs extracted from the reply equals the 5 markers that generate.mjs planted (judge re-scans the fixture)",
      "judge": { "ref": "judge.mjs", "checkId": "todo-locations" },
      "expectedSteps": [5, 10]
    },
    {
      "id": "t03-bash-logagg",
      "category": "bash-dominant",
      "title": "Log aggregation: count payment timeouts",
      "prompt": "Use the shell to count how many lines in logs/app-2026-09-01.log contain the exact string 'ERROR [payment] timeout'. Compute the count with a shell command (grep, findstr or Select-String); do not count manually by reading the whole file. Then write exactly one integer - the count - into the file answers/t03.txt (no other text in the file). Do not modify anything under logs/.",
      "successCriteria": "answers/t03.txt exists and parses to the exact count of 'ERROR [payment] timeout' lines in the log (17)",
      "judge": { "ref": "judge.mjs", "checkId": "answer-file-int", "groundtruthKey": "paymentTimeoutDay01" },
      "expectedSteps": [5, 10]
    },
    {
      "id": "t04-bash-csvagg",
      "category": "bash-dominant",
      "title": "CSV aggregation: delivered orders in the west region",
      "prompt": "Use the shell to analyze data/orders.csv (columns: order_id,status,region,total). Count the rows whose status is delivered AND whose region is west. Compute the count with a shell command; do not count manually. Then write exactly one integer - the count - into the file answers/t04.txt (no other text in the file). Do not modify data/orders.csv.",
      "successCriteria": "answers/t04.txt exists and parses to the exact delivered+west row count recorded in groundtruth",
      "judge": { "ref": "judge.mjs", "checkId": "answer-file-int", "groundtruthKey": "deliveredWestCount" },
      "expectedSteps": [5, 12]
    },
    {
      "id": "t05-write-feature",
      "category": "write-edit-dominant",
      "title": "Small feature: applyBulkDiscount in src/pricing.js",
      "prompt": "Implement a new exported function applyBulkDiscount(cart, rules) in src/pricing.js. Contract: cart is an array of {price: number, qty: number}; rules is an array of {minQty: number, percentOff: number}. totalQty is the sum of qty. Eligible rules are those with minQty <= totalQty; among eligible rules pick the one with the largest minQty, breaking ties by the largest percentOff; if no rule is eligible the percent is 0. subtotal is the sum of price*qty. discount = subtotal * percent / 100. total = subtotal - discount. Return an object {subtotal, discount, total} with every value rounded to 2 decimals using Number(x.toFixed(2)). Do not modify any existing function or export. Add the function to the module.exports.",
      "successCriteria": "node -e behavior check against workspace/src/pricing.js passes 4 fixed cases (no eligible rule / largest minQty wins / empty cart / fractional rounding), tolerance 0.011 per value",
      "judge": { "ref": "judge.mjs", "checkId": "node-behavior-t05" },
      "expectedSteps": [6, 15]
    },
    {
      "id": "t06-mixed-refactor",
      "category": "mixed-multi-file",
      "title": "Multi-file rename: normalizeRegionCode -> canonicalRegion",
      "prompt": "Rename the function normalizeRegionCode to canonicalRegion everywhere it is defined or used under src/ (definition, imports, call sites). The exported behavior must stay identical: same aliases, same 'unknown' fallback. Update src/utils/format.js, src/utils/validate.js and src/report.js as needed. After renaming, verify by running a quick node check that regionRevenue still returns correct sums, e.g. orders with regions 'NORTH', 'west', 'south' and totals 100, 250, 50 must give regionRevenue(orders, 'north') = 100 and regionRevenue(orders, 'west') = 250. When done, reply with one line:\nPT07_RENAME_DONE <number of files changed>",
      "successCriteria": "Zero occurrences of normalizeRegionCode remain under workspace/src, canonicalRegion is present in the three files, and a node behavior check on src/report.js returns the expected sums",
      "judge": { "ref": "judge.mjs", "checkId": "refactor-t06" },
      "expectedSteps": [6, 18]
    },
    {
      "id": "t07-task-orchestration",
      "category": "task-orchestration",
      "title": "Two-step split with subagent delegation via the task tool",
      "prompt": "This is a two-step task. Step 1: analyze data/orders.csv (columns: order_id,status,region,total) to determine which region (north, south, east or west) has the highest total revenue from delivered orders only - that is, rows with status delivered, summed over the total column. Step 2: delegate the file creation to a subagent with the task tool. Instruct the subagent to create the file answers/t07.md containing exactly two lines: 'region: <winning region>' and 'revenue: <winning revenue>' where the revenue is the delivered total for that region rounded to 2 decimals. After the subagent finishes, read answers/t07.md yourself and fix the file if the subagent got it wrong. Finish by replying with one line:\nPT07_ORCHESTRATION_DONE region=<region> revenue=<revenue>",
      "successCriteria": "answers/t07.md names the groundtruth top delivered region and a revenue within 0.05 of the groundtruth value; task-tool usage is recorded as non-blocking evidence",
      "judge": { "ref": "judge.mjs", "checkId": "orchestration-t07" },
      "expectedSteps": [8, 20]
    },
    {
      "id": "t08-read-logqa",
      "category": "read-dominant",
      "title": "Log Q&A: peak ERROR hour on day 02",
      "prompt": "Read logs/app-2026-09-02.log and determine which hour of the day (00-23, the two-digit hour from the timestamp) contains the most lines whose level is ERROR. Determine the answer from the log content itself; do not modify the file. Reply with exactly one line and nothing else:\nPT07_ANSWER_PEAK: <hour as two digits>",
      "successCriteria": "Reply matches PT07_ANSWER_PEAK: <hour> where hour equals the groundtruth peak ERROR hour (14), which is the unique maximum that hour",
      "judge": { "ref": "judge.mjs", "checkId": "reply-substring" },
      "expectedSteps": [5, 9]
    }
  ]
}
